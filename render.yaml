services:
  - type: web
    name: news-brief-desk
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
